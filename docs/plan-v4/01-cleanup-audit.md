# Plan-v4 Week 1 — Cleanup audit

> One-screen audit of files that may be obsolete given the Plan-v4 narrative shift. **No deletions performed.** Every row below is a proposal for the architect to review and action separately, ideally as single-purpose follow-up commits.

Scope of this audit: the public monorepo at the top level, `docs/`, `scripts/`, and the in-tree project directories. The `files/` directory is gitignored (working downloads, not in repo) and not in scope. The `bls-platform-private` repo is also out of scope.

## Findings

| File | Reason it may be obsolete | Proposed action | Justification |
|---|---|---|---|
| `04-llm-gateway/` (whole tree — Chart.yaml, templates/, values.yaml, values-aks.yaml, app/, README.md) | Duplicate Helm chart. Canonical chart is `k8s/workloads/llm-gateway/` and the matrix ApplicationSet (ADR-005) is the single source of truth for delivery. PHASE-2-HANDOFF.md flags this as WU-3 with a five-step deletion gate already drafted. | **Delete** under WU-3 — do not bundle into this PR | WU-3 is a substantive engineering action requiring an orphan-check gate, a values-aware chart diff, and a pre-deletion baseline snapshot. Folding it into a narrative-bootstrap PR would conflate concerns and obscure the diff. Plan-v4 Week 2 closes WU-3 as its own work unit. |
| `04-llm-gateway/README.md` (993 lines) | Embedded in the duplicate chart tree. Contains the project's substantive P4 documentation, which the canonical chart's 52-line scaffold does not replace. | **Move content to WU-9** before the WU-3 delete | PHASE-2-HANDOFF.md WU-9 already scopes this: rebuild a canonical `k8s/workloads/llm-gateway/README.md` with quality-controlled content port, not verbatim copy. Sequence: WU-3 deletion is gated on WU-9 having extracted whatever still applies. |
| `03-aks-multicluster/terraform/.terraform/` and `.terraform.lock.hcl` | Local provider plugin cache committed into the repo. The AKS cluster these were initialised against has been torn down (BLS-STATUS.md: "AKS torn down, state empty"). The lock file is the only piece worth retaining if the WU-5 decision rebuilds AKS. | **Add `.terraform/` to repo `.gitignore` and `git rm` the cached directory** — keep `.terraform.lock.hcl` | `.terraform/` is per-machine cache and never belongs in Git. The root `.gitignore` already ignores `.terraform/` for top-level Terraform; the rule is not catching the nested `03-aks-multicluster/terraform/.terraform/` because that path was committed before the rule landed. Separate to WU-5 — this is hygiene, not a portfolio decision. |
| `03-aks-multicluster/terraform/main.tf` (and siblings) | Terraform that no longer corresponds to live infrastructure. The P3 README screenshots remain valid as artefacts; the live AKS cluster is gone. | **Keep until WU-5 decision** | WU-5 is explicitly "clean up dangling refs OR rebuild AKS — decision pending." Either branch of that decision touches this file. Deleting it now pre-empts the decision; keeping it costs nothing. |
| `docs/BLS-Platform-Operations-Guide.docx` (22 KB binary) | Word document committed to a public engineering monorepo. Binary blobs are not reviewable in diff, not greppable, and not versionable in any meaningful sense. | **Convert to Markdown or delete** | If the content is current, port it to `docs/runbooks/platform-operations.md`. If it is superseded by the ADRs and runbooks already in `docs/`, delete it. Either way, a `.docx` does not belong in this tree. Surface to architect — the conversion is a judgment call about whether the content is still load-bearing. |
| `docs/what-broke.md` (13 KB, last touched 2026-05-01) | Incident log written during the early node-hardening sprint. Still useful as portfolio evidence ("real incidents documented") but the format predates the ADR + runbook convention this repo has since adopted. | **Keep, but cross-link from the bridge document** | The content is good — three idempotency failures from the Ansible role with root-cause analysis is exactly the depth signal the bridge document needs. Re-frame as `docs/incidents/` if a series is planned, or keep as a single doc and link to it from the bridge document's incident-response section. No deletion. |
| `scripts/wu-2/` (preflight.sh, execute.sh, artefacts/) | Tooling for a closed work unit (WU-2 — duplicate Application removal, done 2026-05-13). PHASE-2-HANDOFF.md explicitly says scripts are "retained for reference / future incidents." Artefacts directory is already gitignored. | **Keep scripts; leave artefacts gitignored** | The scripts are reusable diagnostic templates for any future duplicate-Application investigation. The preflight/execute pattern is itself a portfolio signal — surfacing it on the bridge document's "engineering discipline" side is worth more than deleting it for tidiness. |
| `setup-claude-context.sh` (top level) | Bootstrap script for `.claude/local-context.md`, which is gitignored. Useful for the architect's own re-seeding workflow; not load-bearing for the portfolio narrative. | **Keep, but move to `scripts/`** | Top-level executables read as platform entrypoints to a first-time visitor. This is a workstation-setup helper, not a platform entrypoint. Moving it to `scripts/setup-claude-context.sh` improves the root-directory signal for first-time readers. Low priority. |
| Top-level `README.md` (current, 192 lines) | Written before Plan-v4. Anchors P1–P3 only ("3 of 6 projects complete"), uses the older positioning frame, and refers to `projects/01-landing-zone/` while P2/P3/P4/P5 live at top level — layout inconsistency the reader has to absorb. | **Rewrite as part of Week 2/3 narrative pass** | Do not delete or move. Week 1 lays the bridge-document spine; Week 2 expands the bridge document; the top-level README rewrite happens against that baseline so the two documents reinforce rather than diverge. Tracked as a Week 2 deliverable. |
| `projects/01-landing-zone/` vs top-level `02-…`, `03-…`, `04-…`, `05-…` | Layout inconsistency: P1 nested one level deeper than P2–P5. Cosmetic, but visible in the root listing. | **Surface to architect — do not auto-resolve** | Either move P1 to top level or move P2–P5 under `projects/`. Either choice rewrites every README's relative links and every ADR's "Files Changed" path reference. This is an explicit architectural decision (a small one) and belongs in a separate PR with link updates, not as a drive-by in Week 1. |

## Notes on what is **not** in the audit

- **`files/` directory** — gitignored. Local-only download/scratch space. Contains earlier-version master plans (`BLS-master-plan-v3.md`, `bls-master-plan/`) that are working artefacts from the Plan-v3 era, not committed history. No action required.
- **`PHASE-2-HANDOFF.md`** — gitignored. Session-continuity doc; deliberately ignored per the architect's working pattern (matches `BLS-STATUS.md`). Out of scope for cleanup.
- **`docs/plan-v4/orchestration-prompts.md`** — newly gitignored in this PR (was previously caught by the broader `docs/plan-v4/` ignore). The orchestration prompts are local-only weekly source-of-truth; the Plan-v4 directory itself is now committed so the operating doc, this audit, and the concept→tool mapping can ship publicly.
- **ADR-002, ADR-003, ADR-005, ADR-006 and `02-k3s-platform/docs/adr/ADR-004-…`** — all current, all referenced from PHASE-2-HANDOFF.md or BLS-STATUS.md. None obsolete.
- **`docs/runbooks/sealed-secrets-controller.md`** — current, load-bearing for WU-1 closure. Not obsolete.

## Failure-mode flag

None of the proposed actions in this audit would lose unrecoverable context. The two largest blobs (`04-llm-gateway/README.md`, `docs/BLS-Platform-Operations-Guide.docx`) have explicit "port the content first" preconditions. The `.terraform/` cache directory contains no human-authored content. Everything else is "keep" or "move."

---

# Plan-v4 Week 2 — Cleanup audit (appended)

> Re-run at the start of Week 2 against the merged Week 1 baseline. Appended rather than replacing the Week 1 audit so the running log stays intact. **No deletions performed in this audit.** Items actioned in this PR are listed under "Status of Week 1 findings" below.

## New findings (surfaced during Week 2 pre-flight)

| File / location | Reason it may be obsolete or wrong | Proposed action | Justification |
|---|---|---|---|
| `k8s/workloads/llm-gateway/values.yaml:46` (`proxmox.ollamaEndpoint: "http://<ollama-host-ip>:11434"`) | Same homelab IP surfaced in `PHASE-2-HANDOFF.md`'s "Pending post-WU-3 priority chore" — but in an operationally load-bearing values default, not documentation. The README scrub is in scope of this PR (Week 2, override 1); the values.yaml IP is **out of scope** because genericising it to a placeholder would break the live chart's runtime behaviour. | **Surface to architect — handle in a follow-up commit alongside an ADR or as part of WU-4.** Do not auto-scrub. | Two coupled questions: (1) whether the public default should point at a private homelab IP at all (probably not — the default value is a public artefact, the actual endpoint is environment-specific), and (2) where the real value should live (Helm values override file, SealedSecret, ConfigMap loaded from outside the chart, etc.). The right answer is a small ADR or a WU-4-adjacent fix, not a drive-by `sed`. The README scrub does not depend on solving this. **Status: closed in WU-4 (PR #10) — the Ollama endpoint now lives in the `llm-gateway-secrets` SealedSecret as `ollama-endpoint`; no plaintext homelab IP in the values default.** |
| `02-k3s-platform/ansible/roles/node-hardening/defaults/main.yml:96` (`fail2ban_ignoreip: "127.0.0.1/8 <homelab-subnet-cidr> <ollama-host-subnet-cidr>"`) | Discloses the homelab subnet structure (`<ollama-host-subnet-cidr>`) in a committed Ansible defaults file. Same blast-radius category as the README IP: harmless in isolation, undesirable as a pattern given the "scrubbed from CLAUDE.md = scrubbed everywhere" principle. | **Surface to architect — defer to scrub-procedure audit.** Do not auto-scrub. | Defaults files in Ansible roles are intentionally public-by-design — the role is meant to be re-runnable by other operators. Genericising to a variable (`fail2ban_ignoreip: "{{ trusted_cidrs }}"` with a documented `vars/` override pattern) is a small refactor with a small ADR's worth of justification. Belongs in its own commit, not bundled into WU-3. **Status: closed in PR #14 / PR #15 — the file now defaults to `fail2ban_ignoreip: "127.0.0.1/8"` only, with the placeholder pattern documented in a comment above the assignment.** |
| **Process gap** — how did `<ollama-host-ip>` survive the pre-session scrub that removed it from `CLAUDE.md` (commit `1bb7d0b`)? | PHASE-2-HANDOFF.md flags this as the "open process-gap question" — was the original scrub scoped to specific paths, was the grep pattern wrong, or were in-tree READMEs and Ansible defaults simply not in the scrub plan? | **Address before WU-9 closes** — write the scrub procedure as a runbook entry under `docs/runbooks/` so the next sensitive-data review has a known-good procedure. | The IP fix itself is a one-line `sed`. The runbook entry is the engineering work that prevents the next miss. This audit row exists to make sure the runbook entry doesn't get dropped when the README scrub passes. **Status: closed in PR #14 / PR #15 (the cross-tree scrub commits) and the operator-handbook template (PR #16) which now carries the editing + rotation rules. Future scrubs should grep with `git grep -nE "192\.168\.|10\.212\.|<other-homelab-prefixes>"` plus the secret-prefix patterns in the operator handbook §7.** |
| `docs/plan-v4/01-cleanup-audit.md` (this file) | Single audit growing across multiple weeks is now ~70 lines. Still readable. | **Keep appending; reconsider split if it exceeds ~200 lines.** | A running log is more useful than separate weekly files at this scale. Splitting prematurely buys nothing and costs cross-week visibility. |

## Status of Week 1 findings

| Week 1 row | Status going into Week 2 |
|---|---|
| `04-llm-gateway/` whole tree | **Actioning in this PR** as TASK 2 (WU-3). 5-step deletion gate executing now. |
| `04-llm-gateway/README.md` (993 lines) | **Actioning in this PR** as TASK 3 (WU-9). Content captured before deletion per override 2; canonical README rebuilt with quality-controlled port. |
| `03-aks-multicluster/terraform/.terraform/` | **Deferred** — no change. Still gated on WU-5 decision; cleanup hygiene PR can land independently any time. |
| `03-aks-multicluster/terraform/main.tf` | **Deferred to WU-5.** No action. |
| `docs/BLS-Platform-Operations-Guide.docx` | **Deferred** — surface to architect remains open. No action this PR. |
| `docs/what-broke.md` | **Keep** — referenced from bridge document §5 (engineering discipline) and §7 (how to verify). No action needed; the cross-link Week 1 audit recommended is already in place. |
| `scripts/wu-2/` | **Keep** — confirmed retained for reference. No action. |
| `setup-claude-context.sh` (top level) | **Deferred** — low priority. No action. |
| Top-level `README.md` rewrite | **Deferred to Week 3** — Week 2 expands the bridge document; the top-level README rewrite happens against the diagram-complete baseline. Tracked in Plan-v4 README §2. |
| `projects/01-landing-zone/` layout inconsistency | **Deferred** — surface remains open. No action. |

## Notes on what is **not** in the audit (Week 2 additions)

- **`docs/diagrams/00-system-context.mmd`, `07-scope-boundary.mmd`** — Week 1 outputs, both current. The catalogue in `docs/diagrams/README.md` already names them and they are referenced from bridge document §1 and §6. No action.
- **`BLS-PLATFORM-ENGINEERING-GUIDE.md`** — Section 4 placeholders are being filled in this PR (TASK 7). The architect-voice rows (`The decision that made this senior:`, `What I deliberately didn't build:`) remain explicitly blank with `[Architect fills in]` markers per the Week 1 design. Not obsolete; in flight.
- **`docs/plan-v4/concept-tool-mapping.md`** — current. Will be extended in Week 3 per the documented Week 3 prompt; not touched this week.

## Failure-mode flag (Week 2)

The two new findings (`values.yaml:46`, Ansible CIDR) are both surfaced rather than actioned. Both deserve their own commit + ADR-or-runbook entry. Bundling either into this PR would conflate concerns — the WU-3 deletion is a deletion, the WU-9 rebuild is a rebuild, the override-1 IP scrub is a single-purpose 5-line fix. Operational config and Ansible defaults are a third category that the architect should sequence after this PR lands.
