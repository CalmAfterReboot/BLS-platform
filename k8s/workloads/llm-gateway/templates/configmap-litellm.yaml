apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: {{ .Values.namespace }}
data:
  config.yaml: |
    model_list:
      - model_name: local/llama3.2
        litellm_params:
          model: ollama/llama3.2
          api_base: {{ .Values.proxmox.ollamaEndpoint }}
      - model_name: local/mistral
        litellm_params:
          model: ollama/mistral
          api_base: {{ .Values.proxmox.ollamaEndpoint }}
      - model_name: local/deepseek-r1
        litellm_params:
          model: ollama/deepseek-r1:7b
          api_base: {{ .Values.proxmox.ollamaEndpoint }}
    router_settings:
      routing_strategy: least-busy
      num_retries: 2
      timeout: 120
    litellm_settings:
      drop_params: true
      request_timeout: 120
    general_settings:
      master_key: ${LITELLM_MASTER_KEY}
    cache:
      type: redis
      host: redis-service
      port: 6379
      ttl: 3600
